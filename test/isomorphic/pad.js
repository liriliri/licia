var _ = require('../isomorphic'),
    chai = require('chai');

var expect = chai.expect;

describe('pad', function ()
{
    var pad = _.pad;

    /*it('add chars to both sides if length is < min length', function ()
    {
        expect(pad('eustia'), 8).to.equal(' eustia ');
    });*/
});